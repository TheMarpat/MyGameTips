// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAkvYfBqvXuqAi9JR02_PddtEtaJiHBOTY",
    authDomain: "mygametips-e7aee.firebaseapp.com",
    databaseURL: "https://mygametips-e7aee.firebaseio.com",
    projectId: "mygametips-e7aee",
    storageBucket: "mygametips-e7aee.appspot.com",
    messagingSenderId: "206952587797",
    appId: "1:206952587797:web:fc69d46ba0804a1ab9e41c"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
